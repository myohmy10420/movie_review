<nav class="navbar navbar-default" role="navigation">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to "電影評價區", root_path, class: "navbar-brand" %>
    </div>

    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav navbar-right">
        <li>
          <%= search_form_for @q, url: search_path, class: "movie_search" do |f| %>
            <div class="input-group">
              <%= f.search_field :title_cont, class: "form-control", placeholder: "搜尋電影..." %>
              <span class="input-group-btn">
                <%= f.submit "搜尋", class: "btn btn-default" %>
              </span>
            </div>
          <% end %>
        </li>
        <% if user_signed_in? %>
          <li><%= link_to "刊登電影", new_movie_path, class: "active" %></li>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle text--white" data-toggle="dropdown">
              會員中心
              <b class="caret"></b>
            </a>
            <ul class="dropdown-menu" aria-labelledby="dropdownMenu1">
              <li><%= link_to "帳號管理", edit_user_registration_path %></li>
              <li role="separator" class="divider"></li>
              <li><%= link_to "登出", destroy_user_session_path, method: :delete %></li>
            </ul>
          </li>
        <% else %>
          <li><%= link_to "註冊", new_user_registration_path, class: "active" %></li>
          <li><%= link_to "登入", new_user_session_path, class: "active" %></li>
        <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
